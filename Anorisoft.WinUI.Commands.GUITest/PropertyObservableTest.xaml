<Window x:Class="Anorisoft.WinUI.Commands.GUITest.PropertyObservableTest"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Anorisoft.WinUI.Commands.GUITest"
        mc:Ignorable="d"
        Title="PropertyObservableTest" Height="450" Width="800" d:DataContext="{d:DesignInstance local:PropertyObservableTestViewModel}">
    <StackPanel>
        <Button Command="{Binding TestAndCommand}">Test Command</Button>
        <Button Command="{Binding TestOrCommand}">Test Command</Button>
        <CheckBox
            Content="Throw Exception"
            IsChecked="{Binding Condition1}" />
        <CheckBox
            Content="Throw Exception"
            IsChecked="{Binding Condition2}" />

    </StackPanel>
</Window>
